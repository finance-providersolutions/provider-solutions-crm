// Placeholder. Commit 5 wires up cross-org list, org filter, and
// create dialog (which requires picking an organization).
export default function Contacts() {
  return (
    <div className="min-h-full pt-[58px] pb-12 px-6">
      <div className="max-w-6xl mx-auto py-8">
        <h1 className="font-display text-4xl text-text mb-2">Contacts</h1>
        <p className="text-text-dim font-mono text-xs uppercase tracking-[0.12em]">
          Cross-org list + create coming in commit 5.
        </p>
      </div>
    </div>
  );
}
