# STATE — Provider Solutions CRM

A snapshot of where this app is right now. Scope is the CRM only. Cross-app concerns live in the suite-level docs at ps-apps-suite/docs/.

## What this app is

Internal CRM for Provider Solutions, LLC (LOCUMs physician staffing). Second app in a suite that already includes a live financial dashboard. Targets demand (opportunities from hospitals and LOCUMs partners) and supply (recruiting, credentialing, and managing providers). Real auth from day one. Users at launch are Jason and Reed; designed to scale to admin staff within months.

## Current phase

Phases 1 and 2 are shipped end-to-end. The full CRM redesign implementation arc (four slices covering primary-header chrome, two-tier list-page chrome, the Opportunities card swap, and the card swap on the four remaining list pages plus two minimal new detail pages) shipped on top. The thin Supabase-only half of the originally-planned pipeline-aware Home rebuild is now shipped — Home shows pipeline-aware KPIs (open opportunities by stage, active providers, tasks needing attention), the existing recent-activity feed, and a navigation hub. The later Home-enrichment pass with optional QBO actuals is still queued — its inputs (expiring credentials, stalled providers, uncredentialed opportunities) come from Phase 3b/4 and the QBO-Worker integration is no longer bundled into Phase 2.

The Home page is currently a deliberately parked four-variant comparison. The route `/` renders a small segmented toggle (V1 · baseline / V2 · retention / V3 · evolved / V4 · synthesis) above the page body; the toggle persists the choice to `localStorage` under `ps-crm-home-variant` and mounts the corresponding variant component. V1 is the open-opp-scoped matching-engine baseline that originally shipped (Demand / The Match / Supply / Attention / Navigate). V2 keeps that frame and adds filled-opportunity retention — filled opps surface as "active contracts," and the band flags at-risk ones whose placed provider has an expiring, applied, or missing privilege. V3 introduces a six-card top KPI row (Filled / Open / Active providers / Pipeline / Activity 7d / Open tasks, responsive 2-up on mobile and 6-up on laptop) and reworks the matching surface as a literal supply-engine-demand flow plus a diverging-bar State Match Map covering every state with either supply or demand. V4 is the user's synthesis — V3's KPI row, a reworked State Match (demand-only states, per-state Level-2 cards with three stacked bars for supply/open/filled so the win-vs-retain split stays legible at small widths) and a reframed Retention section listing every filled contract with a named per-contract finding (e.g. "Bob Anderson's privilege expires in 80 days" rather than V2's tier counts). No variant is the final Home — the four sit side by side intentionally so the decision can be made later. A future teardown slice will collapse Home to whichever variant wins and remove the others plus the toggle.

The KPI cards in V3 and V4 land users on the relevant list page but currently link to the UNFILTERED list — no list page reads URL state today (Opportunities / Providers / Tasks filters are all local `useState`), so deep-linking to a pre-filtered view is blocked on a separate slice that wires `useSearchParams`-driven filter sync into those three pages. The Home wrapper at `src/pages/Home.jsx` is just the toggle shell; the four variants live in `src/pages/home/HomeV1.jsx` through `HomeV4.jsx`, sharing helpers (`BBox`, `AttentionRow`, `NavigateHub`, the eligibility filter and lifecycle classifier carrying over from SuggestedProviders, plus a new `deriveFilledRisk` and `deriveFilledHealth`) out of `src/pages/home/shared.jsx`. A new aggregate hook, `useAllPlacements` in `usePlacements.js`, is the cross-opportunity sibling to `useAllCredentialing` — one query against the non-cancelled placements rows, bucketed into `byOpportunity` and `byProvider` Maps via `useMemo` — and is what lets Home compute matched-vs-open across every opportunity without an N+1.

Phase 3 — credentialing — is shipped through slices 3a and 3b. 3a delivered the credentialing core — schema for `provider_licenses`, `credentials`, and `facility_privileges`; the private `credentials` storage bucket with signed-URL access; manual credential entry on the provider detail page; a cross-provider expiration roll-up at `/expirations`; computed credential status derived from dates at render time; and the provider detail page as the reference design for detail-page layout. 3b delivered three things on top of that: a pure zero-fetch shift-readiness engine (deriveShiftReadiness) that evaluates a provider against an opportunity's declared requirements; opportunity requirements as actual data (a flat required_items text[] column, a Requirements toggle picker in the opportunity form, a compact readout on opportunity detail); and a catalog-driven provider onboarding checklist with optional document attachments into the existing `credentials` bucket. 3b also restructured the provider detail page — Onboarding moved above Credentialing (matching the onboard-then-credential lifecycle), both sections gained an always-visible centered status indicator, and their bodies sit behind a shared default-collapsed CollapsibleSection. That status-indicator-plus-collapsible-body pattern is now the reference shape the polish pass should extend to the other detail pages.

Phase 4 is shipped through slices 4a and 4b. 4a delivered the read-only matching surface — a cross-provider aggregate credentialing fetch behind a ranked Suggested providers list on the opportunity detail page, consuming the Phase 3b readiness engine. 4b added the Selected lifecycle state and the four-stage lifecycle it sits inside: Eligible → Selected → Applied → Privileged. Two of those stages are opportunity-grain (Eligible is a pure computation; Selected is a placements row for the provider-opportunity pair), and two are hospital-grain (Applied and Privileged derive from facility_privileges rows at the opportunity's hospital and are therefore shared across every opportunity at that hospital). Migration 0009 relaxed the placements.status CHECK to add 'selected' alongside the original five values; 'selected' is the only CRM-authored value, written when a recruiter commits a provider to an opportunity. The same slice rewrote SuggestedProviders to apply three hard eligibility filters (state license, specialty, position type — the 4a specialty-grouping concept is removed entirely), added a new branch to the readiness engine for in-progress facility-privilege applications (PRIVILEGE_APPLIED, soft severity, surfaced as the Applied stage and never folded into the portable overall roll-up), and formalized the rule that a credential, license, or privilege with a granting date present and a blank expiration_date reads as active and non-expiring across all three credential types — uniform behavior because they share deriveCredentialingStatus. Slice 3c (the daily credential-alerts Edge Function) stays parked until provider credentialing data is populated enough for the digest to be meaningful. A lifecycle-visibility slice subsequently surfaced the four-stage lifecycle across three additional surfaces — covered in the next paragraph.

A two-level box convention shipped across detail-page sections, alongside a new cross-grain provider-standing surface (Hospital Standing) and matching propagation across the Opportunity and Organization pages. Level 1 — each section's container fill is the new bg-surface-well token (#061626, a darker "well" shade than the page bg) with a full teal border (border-accent) and the bottom accent-to-transparent gradient rule; the section's always-visible summary line sits inside the container top, above the collapsible body. The teal border is load-bearing because the well's fill-vs-page contrast is intentionally low (~1.06) — without the teal edge the container would blend into the page. Level 2 — individual record cards inside the container use bg-surface (#122540), raised above the well. The --surface-well CSS variable was promoted from experimental to a real registered token (bg-surface-well utility in tailwind.config.js). An exploration of an even-darker variant ("B-prime", --surface-deep #03070f) was tried during the checkpoint and rejected as too extreme; --surface-deep was removed so future sessions don't rediscover it. Records-are-cards is a CONDITIONAL of the convention, not unconditional — Level-2 carding applies when records sit directly in a section container (Org Privilege Roster rows, Org Opportunities rows, the three credentialing record groups), but when records are already nested in an intermediate grouping (the Provider Availability lifecycle tiers each wrap their provider rows in their own CollapsibleSection), the rows stay plain to avoid box-in-box-in-box clutter. The convention is now applied to: Provider Onboarding + Credentialing + Hospital Standing; Organization Privilege Roster + Opportunities; Opportunity Provider Availability. The light-mode counterpart shade (#cfdeec) is provisional — text-on-container contrast for accent labels is ~3.47 in light mode, below WCAG AA, and is tracked as a known issue against the suite-level light-mode tuning pass.

The Provider page gained a new Hospital Standing section at body position 4, directly under Credentialing and grouped with the other provider-standing sections (Onboarding, Credentialing) above the Activity/Tasks workflow sections. It is the cross-grain lifecycle-standing view that replaces the original Phase-2 Placements stub: a hospital-keyed outer-join of the provider's selected opportunities (placements, opportunity-grain) and their privilege standing (facility_privileges, hospital-grain), rendered as one group per distinct hospital the provider touches in either grain. Four cases are surfaced distinctly with their own badge treatment: backed-overlap (selected and Privileged or Applied at the same hospital), privilege-only (privilege at a hospital with no selected opportunity), selected-without-privilege (the load-bearing FLAG case — warning-amber "No privilege" badge in the lifecycle-badge slot plus a warning sub-line elaborating), and selected-with-pending-privilege (muted Pending badge — a privilege row exists but is pre-application). The collapsible inside the Level-1 container is labeled "Standing Details" and defaults to OPEN — a deliberate divergence from Onboarding and Credentialing (which default-collapse) because this section's three-line summary isn't self-sufficient; the payload is the per-hospital detail, so leading with it visible serves the user better. Per-hospital cards show the full hospital name on row 1 (no truncation; the badge was moved to row 2 to free top-row space) and city/state with the status badge on row 2. New hook useProviderPlacements(providerId) is a sibling export in usePlacements.js — provider-scoped, eager-joins opportunity → organization for the title and hospital identity, read-only (no select/unselect, which stays on the opportunity page). Reuses useFacilityPrivileges and deriveCredentialingStatus, plus the now-shared privilegeIsExpiringSoon helper which was extracted to expiration.js since this is its third consumer alongside SuggestedProviders and HospitalPrivilegeRoster. Component lives at components/credentialing/ProviderPlacementsSection.jsx.

The Opportunity page's first section was renamed Overview → Details (fixes the cross-page naming drift previously logged in DESIGN-NOTES) and now uses the shared DetailsCollapsibleHeader one-off matching Provider and Organization Details — default-collapsed, header-less primary-section pattern, bare DetailGrid below when expanded, no card wrapper. The Provider Availability section was wrapped in the B box convention and reordered UP to slot 2, directly under Details. Note: the Opportunity page's resulting section order — Details → Provider Availability → Rate structure → GP modeler → Activity → Tasks → Delete — is INTERIM and completeness-driven. The finished Provider Availability section leads; the still-unfinished Rate structure, GP modeler, and Activity sections were cascaded below in their prior relative order to signal that. Final page ordering is a polish-phase decision; the lead position of Provider Availability is not a permanent IA call. Recorded explicitly in DESIGN-NOTES so polish doesn't mistake it for intent.

The New Opportunity create flow was split. OpportunityFormDialog now collects only the minimum to create and manage the record — Basics (hospital, source partner, title, name, position type, specialty, setting), Location, Dates, and Pipeline (stage, probability, notes) — for BOTH create and edit. Stage now defaults to 'lead' on create (it previously saved null). The rate structure (shift defaults, bill, pay, on-call, travel) and the requirements picker were REMOVED from the form entirely, on edit as well as create, so each data slice has a single writer. Rate data now lives in a new RateStructureFormDialog opened from the opportunity detail page — the first "edit-on-detail" section: the detail-page Rate Structure card shows an unset empty state ("Set rate structure") on a fresh opp and the populated read-only grid with a pencil edit affordance once set. On create, required_items is auto-written to ['license'] (the universal state-license requirement) so a new opp is never born on the readiness engine's empty-requirements "Incomplete" path — the matching engine always has at least the state-license hard filter live. The GP Modeler reads the rate columns, so on an opp with no rates yet it shows a guard ("Set rate structure to model gross profit") instead of a meaningless $0 projection; once rates exist it renders the live modeler as before. No migration — every column already existed.

The Credentialing Requirements input, removed from the create form in the split, returned as its own edit-on-detail section on the opportunity detail page (the second such section after Rate Structure). It sits at slot 2 — between Details and Provider Availability — so the page reads Details (the opportunity) → Requirements (what it demands) → Provider Availability (who qualifies). It has the same unset/populated states as Rate Structure: a "Set requirements" empty state on legacy opps whose required_items predate the default-write, and the populated readout (labels in REQUIREMENT_ITEMS order, " · " joined) with a pencil edit affordance once set. Editing opens RequirementsFormDialog — the seven-item REQUIREMENT_ITEMS controlled-vocabulary toggle picker lifted from the deleted form picker, inheriting the shared Dialog height/footer shape. The state-license item ('license') is PINNED ON — rendered checked + disabled, never removable — because it's the universal hard filter; the other six toggle freely. The RequirementsReadout was pulled OUT of the Provider Availability B-box (where it had been the first child) — verified purely presentational, no programmatic dependency — so Provider Availability is now just the matching surface; the new section's adjacency above it carries the requirements-feed-matching relationship. Writes to opportunities.required_items (text[]), which is load-bearing for the matching engine. No migration.

The opportunity detail page's financial sections were matured (last piece of the opportunity-maturation arc). The GP Modeler was renamed "Opportunity Projection" (file, component, section header, toast; internal compute()/gp-modeler.js/modeling_assumptions kept — they name the math/data accurately). It now uses a hybrid shape: an always-visible per-shift summary (the financial spine — Guaranteed Billable Amt., Variable Billed est. when call-back > 0, per-category travel, Pay to provider, → Gross Profit + GP% as the hero) reading SAVED modeling_assumptions, above a default-collapsed body holding ONLY the live assumption-input calculator (live recompute + Save/Discard/Reset + a dirty-state guard that prevents collapse-while-dirty from silently discarding). Rate Structure became a facts-only daily snapshot: grouped Income (reg daily billed + on-call billed → daily total billed) and Expenses (reg daily pay + on-call pay → total pay to provider) with a rate-only daily profit at the bottom — every value a rate column or sum of rate columns, ZERO modeling_assumptions reads and no compute() call (the deliberate single-direction boundary: facts on Rate Structure, modeled projections in Opportunity Projection, so Rate Structure never moves when the modeler changes). Both sections, plus Requirements, now sit in the B-box convention. A financial color grammar applies across them: income green, expense red-in-parens (accounting style), profit + GP% sign-driven (the new --profit teal when >= 0, danger-red-parens when underwater), travel-when-hospital-covers shown as a muted footnote only. Estimate-vs-fact now rides italic (projected = italic, rate facts = plain), freeing color for the income/expense/profit axis. No migration.

Two financial home variants and a new page shipped on top of the opportunity-maturation arc, surfacing Opportunity Projection data on Home. V5 ("financial") shows top-5 Filled and top-5 Pipeline opportunities ranked by projected annual GP; V6 ("can't staff") inverts the lens to surface OPEN opportunities that can't be staffed — zero ready providers after the matching engine's hard filters — with a per-row diagnostic naming the actual gap. Both are additive home-comparison variants (V1–V4 untouched) reusing a shared ProjectionRow + projectionShared helpers; the per-opp compute() and the readiness loop (the same useAllCredentialing + deriveShiftReadiness loop V4 already runs) are a single aggregate fetch + one useMemo, no N+1. A new /financial-projections route+page (flat nav entry) shows the full ranked Filled / Pipeline lists plus a "Not Yet Modeled" group so created-but-unrated opps don't vanish. The shared row gained position_type (leading the title line so it survives truncation) and a context-dependent secondary (target shifts/year on the home widgets — the volume denominator for annual GP; margin % on the page). The financial color grammar (income green / expense red-parens / profit+GP% sign-driven --profit teal) carries onto these surfaces. No migration.

Two shared-component changes landed alongside. TierKPICard's elevation treatment flipped to be well-aware: focused card now uses bg-surface (forward/raised) plus the teal accent border, unfocused cards use bg-surface-well (recedes into the well). This corrects an inversion that appeared once the tier KPI cards sat in dark-well containers — the previously-bg-surface2 unfocused cards visually popped while the barely-tinted focused card receded. The focused-state logic is unchanged (focus lights on card-click or default-open highest-non-empty tier, clears on Expand All or chevron-driven multi-open); only the static fills changed. Shared across both consumers (Provider Availability on the Opportunity page + Privilege Roster on the Organization page), and both improved together. The privilegeIsExpiringSoon helper extraction noted above is the second shared-component change — the lift to expiration.js was triggered by the third consumer and means both SuggestedProviders and HospitalPrivilegeRoster now import it rather than maintaining inline copies.

Provider Availability has been restructured from four lifecycle-progression tiers to three orthogonal-axes tiers. Tier grouping now conveys selection intent for THIS opportunity; the row-level badge carries privileging progress at the hospital as an orthogonal signal. The three tiers are Selected for Placement (anyone selected for this opp, regardless of privileging state), Suggested / Eligible (eligible providers not selected — including providers privileged or applied at the hospital but not selected for this specific opp, since those are positive eligibility signals rather than selection signals), and Blocked (verdict-fallback). The KPI strip dropped from four cards to three (Selected / Eligible / Blocked); the previous "Privileged & Ready" first tier retires from this surface. Inside the non-Blocked tiers the badge reads Privileged, Applied, or Eligible based on the provider's privilege state at the hospital — the "Selected" badge label is gone because tier grouping carries the selection signal. Within-tier sort surfaces privileged > applied > none then by name, so ready-to-deploy selections head the Selected tier and the strongest selection candidates (already privileged here) head the Suggested / Eligible tier. The left-border accent on selected rows was dropped — tier grouping makes it redundant. HospitalPrivilegeRoster on the Organization detail page is untouched — its tier semantics (hospital-keyed Privileged + Applied across providers, no selection concept) are separate and remain unchanged. The previously load-bearing "privilege wins over selection" precedence rule is superseded; DESIGN-NOTES retains the prior entries with SUPERSEDED markers for historical context.

Lifecycle visibility shipped on top of Phase 4b, surfacing the lifecycle in three places. On the opportunity detail page the matching list became a "Provider Availability" section. The original shape was a row of four compact KPI count cards (Ready, Selected, Eligible, Blocked) above four collapsible tiers in lifecycle order (Privileged & Ready, Selected + Applied, Eligible, Blocked); the cluster-A polish-pass restructure has since flattened this to three orthogonal-axes tiers (Selected for Placement, Suggested / Eligible, Blocked) — see the orthogonal-axes paragraph above for the current shape. Dual-mode interaction — clicking a KPI card opens that tier and collapses all others (accordion focus, with the clicked card showing an accent focused-state), while per-tier chevrons remain independent manual toggles so multiple sections can be open at once; chevron-opens and an "Expand All" button clear the card focus deliberately because the "this is the focused tier" claim no longer holds when several are open. Default on load: the highest-ranked non-empty tier opens with its card accented; empty tiers do not render their collapsible body though the count card still shows zero. On the hospital organization detail page, gated on org.type === 'hospital', two new sections sit between Details and Contacts, both wrapped in the standard surface card — a Privilege Roster (providers Privileged or Applied at THIS hospital — NOT Selected, which is opportunity-grain per the placement boundary; same compact KPI plus collapsible tiers plus accordion pattern, with expiring-soon flagged via the existing privilege-expiry logic) and an Opportunities list (the hospital's opportunities, inline-filtered from useOpportunities by organization_id, each linking to its detail page). The Eligible lifecycle stage uses white (text-text) rather than green so it reads distinctly from Privileged — teal is already saturated in the Suggested Providers area (provider names, the Selected label, chip, and left border) and would have collided. On mobile (below md) the provider rows in the opportunity tiers drop the redundant position and specialty meta (those are constant across the filtered list) and keep home state alone on a lower line with the status indicator right-aligned alongside it; desktop keeps the full position·specialty·state meta and the original status position. Three shared-component changes landed alongside the surfaces: the provider page's one-off Details collapsible header was extracted into a shared component now used identically by both the provider page and the org page Details (the org Details previously used the labeled-sub-group CollapsibleSection by mistake; it now uses the header-less primary-section one-off, matching the provider page); CollapsibleSection gained an optional controlled mode (open + onOpenChange props, backwards-compatible — uncontrolled consumers stay unchanged) so the opportunity and roster tier groups can coordinate the accordion from a parent; and a compact TierKPICard sibling was added alongside the brand KPICard (same file, shared VALUE_COLOR) for the four-across-on-mobile tier count cards. A small local fix piggybacked on the org-page work: the Contacts SectionHeader's stale wrapper div was removed so its text re-centers between the rules; the shared SectionHeader was confirmed fine and untouched.

A read-only recruiting-funnel view at `/funnel` is also shipped, on top of the new 10-stage provider status. It groups providers by stage so the supply pipeline reads at a glance, with no schema change — purely a new presentation of data already in the providers table.

## Hosting and infrastructure

The app is hosted on Cloudflare via the provider-solutions-crm project. The deploy config lives in wrangler.jsonc. As of the iOS PDF fix, the deployment is no longer pure static assets — a small Worker script at worker/index.js fronts the assets binding, handles a single dynamic API route under /api/*, and falls through to env.ASSETS.fetch(request) for everything else. The build still runs vite and uploads the dist/ folder; Cloudflare's single-page-application not-found-handling preserves SPA routing through the ASSETS fallthrough. The legacy public/_redirects file was removed once the wrangler config took over. The one live API route today is GET /api/onboarding-template/:item_key, which looks the item_key up in onboarding_item_types, mints a 5-minute Supabase signed URL against the credentials bucket using the service-role key, and returns a 302 redirect to it — added so the Download blank template link works on iOS Safari and Chrome, which block the previous client-side createSignedUrl + window.open pattern as an out-of-gesture popup. The service-role key is a Cloudflare Workers secret named SUPABASE_SERVICE_ROLE_KEY, set out-of-band via wrangler secret put and never committed; the browser bundle continues to use only the publishable key. Deploys are git-push-triggered: pushing to the connected branch causes the Cloudflare dashboard to run npm run build then npx wrangler deploy server-side. No local Cloudflare auth is needed; the production URL is confirmed against the Cloudflare dashboard after each deploy completes.

Backend is Supabase (single project, all environments share it for now). Supabase project ref is ztbadmaufcpkinnjztxy; the URL and publishable key are hardcoded in src/api/supabase.js, which is safe because Row Level Security is enabled on every table. The service role key never ships to the browser and lives only in the Supabase dashboard or in Jason's terminal env when running scripts.

The app is PWA-capable. index.html declares the manifest and Apple home-screen meta tags; public/manifest.webmanifest sets standalone display, dark theme color #0b1c2e, and uses pslogo.png as the icon. iOS safe-area-inset-top is respected on the fixed page header and on every page's top padding so the app sits below the status bar when installed.

When the CRM needs financial-dashboard data (QBO actuals against opportunities), the plan is to call the existing worker at qbo-proxy.finance-providersolutions.workers.dev rather than duplicating that data into Supabase. No CRM code calls that worker yet. That integration is now optional and deferred to a later Home-enrichment pass — gated on whether seeing opportunity-vs-actuals would change a weekly decision — and is no longer bundled into Phase 2.

## File and module inventory

Top-level config: package.json (locked stack — React 18, Vite 5, Tailwind 3, react-router-dom 6, shadcn/ui via Radix primitives including avatar, progress, command, and popover added in Phase 2, sonner for toasts, lucide-react for icons, plus xlsx added in Phase 2 for the AppSheet import), tailwind.config.js (maps utility names to CSS variable tokens), components.json (shadcn config), jsconfig.json (the @ alias resolves to src/), vite.config.js, postcss.config.js, .env.example, wrangler.jsonc, .gitignore.

Reference docs in the repo root: BUILD_PLAN.md is the source of truth for data model and phasing. CLAUDE.md gives Claude Code session conventions. PHASE_1_KICKOFF.md and PHASE_2_KICKOFF.md are the historic Claude Code briefing prompts. README.md is the human onboarding doc. The v1 archives (BUILD_PLAN_v1_vanilla_archive.md, CLAUDE_v1_vanilla_archive.md, PHASE_1_KICKOFF_v1_vanilla_archive.md) capture the abandoned vanilla-JS multi-page version and are read-only history.

Public assets: public/pslogo.png is the brand mark, public/manifest.webmanifest is the PWA manifest.

Source tree under src/. main.jsx mounts ReactDOM. App.jsx wires ThemeProvider, then AuthProvider, then BrowserRouter, then fourteen routes (login, home, organizations list, organization detail, contacts list, contact detail, providers list, provider detail, opportunities list, opportunity detail, tasks list, task detail, activities archive, expirations roll-up) plus the funnel and financial-projections pages noted elsewhere — every protected route is wrapped in RequireAuth and rendered inside an AppShell that draws the fixed PageHeader. index.css imports Google Fonts (DM Serif Display, DM Sans, DM Mono), then styles/tokens.css, then the three Tailwind directives.

styles/tokens.css holds every CSS variable the app uses, in two blocks — dark default and light override under data-theme="light". It is the single source of truth for color and radius. The dark-default block carries --surface-well (#061626, a darker "well" shade than the page bg) alongside the existing --bg, --surface, --surface2 elevation tokens; --surface-well is the Level-1 container shade for the two-level box convention applied to every standing-cluster detail-page section. The light-mode block carries a provisional --surface-well (#cfdeec) that still needs visual tuning during the suite-level light-mode pass — its accent-on-container contrast is below WCAG AA. tailwind.config.js registers the matching bg-surface-well utility alongside bg-surface and bg-surface2 so the token is consumable as a normal Tailwind class. shadcn's expected token aliases (background, foreground, primary, card, popover, muted, destructive, ring) are computed from the brand values so primitives Just Work.

api/supabase.js exports a singleton supabase client configured with persistSession, autoRefreshToken, and detectSessionInUrl turned on.

context/AuthContext.jsx wraps supabase.auth and exposes session, user, requestEmailOtp, verifyEmailOtp, signOut, loading. context/ThemeContext.jsx persists the theme to localStorage under the ps-crm-theme key.

components/auth/RequireAuth.jsx redirects to /login when there is no session and shows a brief loading indicator while the listener attaches.

components/brand/ contains the hand-mirrored brand components: PageHeader (fixed top bar with logo, title, optional subtitle, nav button — the subtitle span renders only when the prop is provided so the wordmark stands alone otherwise; the app currently passes no subtitle), Navigation (slide-in drawer with route links and ThemeToggle), ThemeToggle, KPICard, SectionHeader. KPICard's label, value, and sub slots use one-line min-heights so short content sits tight with no reserved empty rows; slots grow to two lines when content genuinely wraps, and cross-card height equality in a row comes from the grid (default align-items: stretch) so when one card grows the others stretch to match. The card itself is horizontally and vertically center-aligned, so when a row stretches, shorter siblings vertically center their content within the matched height rather than going ragged. A compact TierKPICard sibling lives in the same file (sharing the VALUE_COLOR map) for tier-count rows that need four cards across at 380px — same visual grammar at quarter the footprint (no sub slot, tighter padding, smaller fonts), accepts an onClick + a focused state for the accordion-focused tier card. The TierKPICard's elevation treatment is well-aware: focused card uses bg-surface (forward/raised) with a teal accent border + inset accent ring; unfocused cards use bg-surface-well (recedes into the well container around them). This corrects the previously-bg-surface2 unfocused vs bg-accent-dim focused treatment that visually inverted once the cards sat in dark-well section containers. Shared across both consumers (Provider Availability on the Opportunity page + Privilege Roster on the Organization page) — both depend on the same elevation behavior. The original KPICard is unchanged; Home is its only consumer. Visual structure is mirrored from the dashboard at ../ps-app-dashboard/src/components/.

components/ui/ is the shadcn primitives folder: alert-dialog, avatar, badge, button, command, dialog, dropdown-menu, input, label, popover, progress, select, sonner, table, tabs, textarea. Avatar and progress were added for the image-upload UI; command and popover were added for the searchable hospital and source-partner combobox in the opportunity form; alert-dialog was added during the normalization slice to back the shared ConfirmDeleteDialog wrapper. Four custom wrappers live alongside the primitives — all CRM-only abstractions, not shadcn primitives. ConfirmDeleteDialog standardizes destructive-action confirmations with a brand-styled AlertDialog and focus restoration to the trigger element. CardKebab standardizes the per-card / per-row actions affordance introduced in Slice 3 and reused across Slice 4: a MoreVertical trigger button that stops both click and keydown propagation so taps don't bubble into the card's onClick, plus a dropdown menu with Edit + Delete in fixed order. An extraItems prop accepts additional lifecycle actions to render between Edit and Delete — destructive Delete is always last. Providers cards use extraItems to carry Archive/Unarchive; the four other list pages pass no extraItems and get the standard Edit + Delete pair. CollapsibleSection is the labeled-sub-group collapsible added in Phase 3b — a small in-house wrapper (label + children, chevron rotates -90° collapsed / 0° expanded) shared by Onboarding, Credentialing, the four opportunity Provider Availability tier sections, and the two hospital Privilege Roster tier sections. It supports both uncontrolled mode (defaultOpen) and an optional controlled mode (open + onOpenChange), the latter added so a parent can coordinate the tier groups' accordion behavior; the uncontrolled-mode consumers (Onboarding, Credentialing) are unchanged. Still deliberately not a generic accordion system. DetailsCollapsibleHeader is the fourth wrapper, extracted from the provider page during the lifecycle-visibility slice — the header-less primary-section pattern used at the top of detail pages (chevron + bold mono cap accent label + gradient rule fading right). It now backs both the provider detail page's Details and the organization detail page's Details so the two surfaces match; the org page previously used CollapsibleSection by mistake, which was the wrong shape (sub-group, not primary-section) and has been corrected.

Feature-scoped components: components/organizations/OrganizationFormDialog.jsx (now also accepts an optional initialValues prop so the inline-create flow from the opportunity combobox can pre-seed type=hospital), components/organizations/HospitalPrivilegeRoster.jsx (the hospital-only Privilege Roster section on the organization detail page — filters useAllCredentialing privileges by organization_id, joins useProviders for names and photos, groups into Privileged and Applied tiers and renders the same TierKPICard plus collapsible accordion pattern used on the opportunity page, reusing the shared privilegeIsExpiringSoon helper from expiration.js for the within-90-days warning treatment; the per-row treatment is the Level-2 bg-surface record card per the B box convention, nested inside the section's bg-surface-well container), components/organizations/HospitalOpportunityList.jsx (the hospital-only Opportunities section — inline-filters useOpportunities by organization_id, no new hook; rows render as Level-2 bg-surface cards matching the Privilege Roster), components/contacts/ContactFormDialog.jsx, components/activities/ActivityFeed.jsx and LogActivityForm.jsx (the latter is polymorphic — accepts parentColumn and parentId so the same form drives organization, opportunity, and provider activity logging; ActivityFeed's loading / empty states render as plain muted text on the page background, not a card, so an empty section never reads as a record), components/uploads/ImageUpload.jsx (drag-drop + progress + size/type validation for public-read buckets) and components/uploads/Thumb.jsx (thumbnail with initials fallback) and components/uploads/DocumentUpload.jsx (sibling for the private credentials bucket — accepts PDF + images, lazy-fetches a fresh signed URL on each "View" click so a long-open form doesn't end up with a stale link), components/providers/ProviderFormDialog.jsx (full provider create/edit with photo upload), components/providers/ProviderCard.jsx (shared provider card used by both the Providers list and the Funnel — accepts a showStatus prop to suppress the pipeline-status badge when redundant, and treats the edit/archive/delete handlers as optional so the kebab disappears entirely in read/navigate-only contexts) and its companion components/providers/statusBadge.js (the STATUS_BADGE palette + fallback, moved out of Providers.jsx; the page re-exports for back-compat), components/opportunities/OrganizationCombobox.jsx (searchable picker over orgs of a given type with optional inline-create), components/opportunities/OpportunityFormDialog.jsx (sectioned create/edit with basics, location, dates, shift defaults, bill rates, pay rates, on-call, travel costs, pipeline, and Requirements — a list of toggle rows backed by REQUIREMENT_ITEMS that writes the selected keys into opportunities.required_items), components/opportunities/GPModeler.jsx (the live gross-profit projector), components/opportunities/SuggestedProviders.jsx (Phase 4a + 4b, reorganized in the lifecycle-visibility slice, restructured again in the cluster-A polish-pass tier-restructure — the matching list rendered as the "Provider Availability" section on the opportunity detail page; composes useProviders filtered to non-archived plus useAllCredentialing plus usePlacements plus the loaded opportunity, applies three hard eligibility filters — a non-withdrawn state-license row for the opportunity's state in any status, specialty equals the opportunity's, position type equals the opportunity's — then runs deriveShiftReadiness on the surviving providers, splits derivation into two pure functions (computeGroup returning 'selected' / 'eligible' / 'blocked' and computePrivilegeProgress returning 'privileged' / 'applied' / 'none'), and renders the surviving providers as a row of three compact TierKPICards (Selected / Eligible / Blocked) above three collapsible tiers (Selected for Placement, Suggested / Eligible, Blocked) in the controlled mode of CollapsibleSection. Tier grouping conveys selection intent for this opportunity; the row-level badge carries privileging progress at the hospital as an orthogonal signal (Privileged / Applied / Eligible inside non-Blocked tiers, with verdict-fallback labels inside Blocked). The component owns a single tierOverride state — null until first interaction so defaults flow through — that resolves to { open: Set, focused: key|null }. Card click focuses one tier and closes others (or closes if clicking the focused one); chevron toggles act independently and clear focus; an Expand All button above the cards opens all non-empty tiers, also clearing focus. Default load opens the highest-ranked non-empty tier with its card accented. Each row shows photo, name, privilege-progress badge (or verdict-fallback label inside the Blocked tier), top reason line when relevant, plus a Select / Un-select action button that writes through usePlacements with a confirm step on un-select. Selection mechanics are unchanged from the previous structure — the button toggles between Select and Un-select on the placement record; the tier the row lands in is computed from the new selection state on re-render. The row layout is responsive: mobile drops position+specialty (constant across the filtered list) and shows home state on a lower line with the status indicator right-aligned alongside it; desktop keeps the full position·specialty·state meta with status inline next to the name. Within each non-Blocked tier, rows sort by privilege-progress (privileged > applied > none) then name, surfacing ready-to-deploy rows at the top. Eligible's badge tone is text (white) rather than income (green) because teal is already saturated in this section (provider names, the focused-tier accent) and green would have collided with Privileged. Empty-state copy: 'No providers are currently eligible for this opportunity.' when filters resolve to zero, and 'No state is associated with this opportunity.' when the opportunity's hospital has no state), components/tasks/TaskFormDialog.jsx (task create/edit with a parent picker that radio-toggles between organization, opportunity, provider, or no parent — three inline Command-in-Popover comboboxes backed by their respective resource hooks), components/tasks/TaskCard.jsx (the shared TaskCard extracted from Tasks.jsx during the cluster-A polish-pass; both layouts — stacked mobile, single-row wide — live here, parameterized by a hideParent prop that collapses the parent block on detail-page consumers where the parent IS the page, and consumed by both /tasks with hideParent=false and TasksSection with hideParent=true), and components/tasks/TasksSection.jsx (embedded parent-scoped task list — replaced the prior table-based TaskMiniTable with a card stack of the shared TaskCard, carries a "Show completed (30d)" toggle and a "View all" button next to "+ New task" that routes to /tasks for the unbounded list, with empty/loading/error states rendering as plain muted text, not a card, matching the credentialing-section grammar). The credentialing folder (components/credentialing/) holds the Phase 3 surface — both 3a (manual entry, expiration tracking) and 3b (readiness engine, restructured provider detail). ExpirationCluster.jsx and its companions (the amber countdown pill, the shouldShowExpirationPill predicate) are the suite's reference date pattern. LicenseFormDialog.jsx, CredentialFormDialog.jsx, FacilityPrivilegeFormDialog.jsx are the form dialogs — no manual status picker, status is computed at render time from application/granting/expiration dates. LicensesSection.jsx, CredentialsSection.jsx, FacilityPrivilegesSection.jsx are the embedded add/edit/list components; they are no longer rendered directly from Provider.jsx but instead live inside the new CredentialingSection.jsx wrapper, which renders the worst-status-per-group summary and the shared CollapsibleSection above them. expiration.js (deriveCredentialingStatus + derivedStatusLabel + derivedStatusToneClass + expirationBucket + daysUntil + statusForInsert + privilegeIsExpiringSoon + PRIVILEGE_TERMINAL_STATUSES) is the one helper module driving the card cluster, the cross-provider dashboard, AND the worst-status-per-group summary in CredentialingSection — date math is never reimplemented anywhere. privilegeIsExpiringSoon is the 90-day-window predicate for facility-privilege rows; it was extracted out of two near-identical inline copies (SuggestedProviders + HospitalPrivilegeRoster) when the new ProviderPlacementsSection became the third consumer. FacilityPrivilegesSection's kebab carries "Mark denied" / "Mark withdrawn" / "Clear outcome" actions because those terminal outcomes can't be derived from dates and override the date-driven display. OnboardingSection.jsx renders the provider onboarding checklist — the always-visible "N of N complete" status line + progress bar above the shared CollapsibleSection, then the catalog-driven list inside it; single-persisted items (cv, background_check, the new information-shaped emergency_contact and malpractice_coi, and the four new document-template items) use a toggle-creates-row-already-done shortcut on first tap, repeatable items (references) render zero-to-many with add and per-row delete. The completion denominator is now catalog-driven — one slot per non-repeatable item plus one slot per repeatable group (any one row done satisfies the group) — so new catalog rows extend it automatically without a code change. Catalog rows that carry a template_path (the four signature documents — PSA, Attestation, W-9, Direct Deposit) render with a "Download blank template" affordance stacked above the existing upload control; the download lazy-fetches a fresh 5-minute signed URL on click against the credentials bucket, matching the staleness-safety pattern DocumentUpload's View action already uses. Onboarding NO longer carries derived license/DEA rows — that information now lives solely in the credentialing summary, so it isn't shown twice on one screen. ProviderPlacementsSection.jsx is the new cross-grain Hospital Standing section on the provider detail page (body position 4, under Credentialing) — composes useProviderPlacements + useFacilityPrivileges and outer-joins them on hospital, rendering one Level-2 bg-surface card per distinct hospital the provider touches; each card surfaces the per-hospital privilege state plus any selected opportunities at that hospital, with a warning-amber "No privilege" badge + sub-line for the load-bearing selected-without-privilege flag case. The collapsible inside is labeled "Standing Details" and defaults to OPEN — diverges from Onboarding/Credentialing (default-collapsed) because the three-line summary isn't self-sufficient. Replaces the original Phase-2 Placements stub. readiness.js exports deriveShiftReadiness — a pure zero-fetch function that scores a provider against an opportunity using already-loaded data; it reuses deriveCredentialingStatus from expiration.js, never reimplements date logic, and returns a structured result with an overall portable-only roll-up, a facility companion that is reported but never folded into overall, the resolved licensure state, and a flat machine-readable reasons[] array with codes (REQUIREMENTS_UNDEFINED, STATE_UNKNOWN, LICENSE_MISSING_FOR_STATE, LICENSE_EXPIRED, CORE_MISSING, CORE_EXPIRING, PRIVILEGE_NONE, PRIVILEGE_APPLIED, PRIVILEGE_DENIED, PRIVILEGE_EXPIRED). Requirements gate every dimension — a dimension not in the opportunity's required_items is skipped, not blocked. Roll-up precedence is blocked > indeterminate > expiring > ready; an expiring required component flips overall to expiring. The portable-vs-facility split exists so an unplaced candidate without a privilege row at the hospital does not read as uniformly blocked. Phase 4b added a distinct facility-dimension status 'applied' (in addition to ready, expiring, blocked) with its own code PRIVILEGE_APPLIED at soft severity for in-progress privilege applications — an application_date set with no approval_date yet at the opportunity's hospital. Like every facility state it is reported as a companion and never folds into overall. The permanent-credential rule is uniform across all three credential types because they share deriveCredentialingStatus: a row with a granting date present and a blank expiration_date reads as active and non-expiring (not expired, not missing); a row with both dates blank stays pending. A permanent row is intentionally absent from the /expirations renewals view (it has nothing to renew) and remains findable on the provider detail page's credentialing section.

Hooks under hooks/ — one per resource. useAuth.js exposes the auth context. useOrganizations.js owns list and singular useOrganization(id) with create, update, remove. useContacts.js takes an optional organizationId to scope and selects the parent organization eagerly (with city, state, and logo_path so the wide-layout card cluster can render org name plus location); useContact(id) is the singular fetch for the Contact detail page. useActivities.js takes optional organizationId, contactId, opportunityId, providerId, sinceDays, and limit filters and selects parent organization and contact eagerly. The opportunityId key was added in the cluster-A polish-pass Activity-propagation sub-arc — it was previously absent from the destructure, so the Opportunity detail page's activity feed had been silently fetching unfiltered; it now scopes correctly to this-opp activity. useProviders.js mirrors the same shape and exposes both a list hook and a singular useProvider(id) for detail pages. useOpportunities.js eagerly joins both the parent organization and the optional source partner organization in a single select using explicit FK-constraint disambiguation, since opportunities references organizations twice. useTasks.js takes optional assigneeId, status, and completedSinceDays filters, eagerly joins all three optional parents (organization, opportunity, provider) — the opportunity sub-select also nested-joins its parent hospital name via the opportunities_organization_id_fkey constraint so a task linked to an opportunity can render the hospital underneath the opportunity title on cards and the detail page — and exposes a quickComplete(id) action that sets status='completed' and completed_at=now() in one call, the load-bearing action for the open-task list views. useTask(id) is the singular fetch for the Task detail page. useCredentialing.js (slice 3a) exports three sibling hooks scoped to a provider_id: useProviderLicenses, useCredentials, useFacilityPrivileges — same shape as the others, ordered by expiration_date ascending with nulls last; the privileges hook eagerly joins the parent hospital organization for the row's logo and name. useExpirations.js (slice 3a) feeds the /expirations roll-up — fetches all three credentialing tables in parallel, returns a flat normalized item list, and exports a sibling bucketExpirations helper that groups the list into past / 30 / 60 / 90 day windows; the page applies search/sort/type filters and re-buckets in a useMemo. useOnboarding.js (slice 3b) is the sibling for the onboarding tables — returns the catalog (onboarding_item_types, ordered by sort_order) plus the provider's persisted onboarding_items in one pair of parallel fetches, with create/update/remove and a toggle convenience that flips done and stamps or clears completed_date in a single round-trip. useMatching.js (slice 4a) is the cross-provider sibling to useExpirations — exports useAllCredentialing, which runs three parallel queries against provider_licenses, credentials, and facility_privileges with no provider_id filter and no expiration filter, then buckets each result set into a Map keyed by provider_id via useMemo and returns licensesByProvider / credentialsByProvider / privilegesByProvider plus loading / error / refetch. It is the aggregate-fetch shape matching needed that the single-provider credentialing hooks could not feed without an N+1 loop, and the same hook supports a future cross-opportunity matching surface with no rework. usePlacements.js (slice 4b) is the opportunity-scoped placements read/write hook — returns non-cancelled placement rows for the given opportunity plus selectProvider(providerId) and unselectProvider(placementId). selectProvider writes status='selected' with start_date / end_date / pay_rate / bill_rate left null under the placement boundary and enforces at-most-one non-cancelled placement per (provider, opportunity) pair in the app layer via a read-then-insert check (no DB unique constraint so the future Scheduler can later create non-Selected rows for the same pair); unselectProvider hard-deletes. usePlacements is the first code in the app ever to write to the placements table — the table was created as a Phase-2 stub and sat unused until 4b. usePlacements.js also exports the sibling useProviderPlacements(providerId) — provider-scoped read for the Hospital Standing section on the provider page, eager-joins opportunity → organization via explicit FK-constraint disambiguation so each row carries the opportunity title plus its parent hospital identity in one fetch. Read-only — no write actions; commit/un-commit happens on the opportunity page. None of these hooks use React Query — each owns its own loading, error, data, refetch.

Pages under pages/. A cluster-A polish-pass universal threads through every detail page touched in this sub-arc: Activity sections use the "+ New activity" toggle pattern (reveals LogActivityForm on demand) with the feed defaulting to the last 90 days via useActivities sinceDays and a "View all" button routing to /activities for the unbounded archive — applied to Provider, Organization, and Opportunity in one pass. Tasks sections render the shared TaskCard with hideParent={true} (the parent IS the page) and carry a "View all" button routing to /tasks. Details sections use a horizontal label-value rhythm on all five detail pages — label LEFT in a fixed w-32 column with mono caps, value RIGHT next to it, gap-y-1.5 row spacing — converging the five previously-divergent local DetailGrid/DetailField copies onto the same shape (extraction to a shared component is queued). Contact and Task Details still sit inside their existing bg-surface card wrapper; Provider, Organization, and Opportunity use the bare DetailsCollapsibleHeader + grid pattern with no card wrapper. Empty-state shape across the suite is plain text, text-text-muted, font-mono text-xs uppercase tracking-[0.1em], centered py-6 — most consumers already conformed; remaining outliers are tracked for their own sub-arc passes. Login.jsx is a two-step OTP form (email step, then code step). Home.jsx is the thin pipeline-aware dashboard — opens straight into three center-aligned KPI cards (open opportunities with full-name stage breakdown that hides zero-count stages, active providers, tasks needing attention with overdue red treatment when at least one open task is past due), the shared ActivityFeed (last 10), and a six-entry navigation hub (Organizations, Opportunities, Providers, Contacts, Tasks, Expirations) matching the Navigation drawer order. Home has no in-page title block and no two-tier fixed header — the primary PageHeader is the only chrome above the content; the two-tier pattern was deliberately not added because Home has no search/filter/+new action to fill its right-side slot, and a fixed bar with an empty action group reads worse than no bar at all. Definitions reuse a single source of truth — the "open opportunity" stage allowlist lives in constants as ACTIVE_OPPORTUNITY_STAGES and is consumed by both Home and the Organizations card pill. The QBO-actuals and action-item cards (expiring credentials, stalled providers, uncredentialed opportunities) are deferred to the later Home-enrichment pass. Expirations.jsx (slice 3a) is the cross-provider credentialing-renewals roll-up at /expirations — uses the suite's two-tier list-page chrome (bar-2 with title + description + Search/Filter/Refresh icons, conditional bar-3 search, slide-in filter sheet with Type + Sort) and groups items into four bucket sections (Past expiration / Next 30 days / 31-60 days / 61-90 days); each row links to the parent provider's detail page; rows are read-only (entry happens on the provider page). Inline expiration cluster on desktop, compact pill-only on mobile. All five list pages now use the responsive card pattern instead of a shadcn Table — see the design-system section for the shared card grammar and the per-page variations. Organizations.jsx has Type and Sort filters and a create dialog; Organization.jsx is the detail page. Its header was restructured in the cluster-A polish-pass to match the Provider and Opportunity pattern: a FIXED condensed header below the primary PageHeader with a square logo thumb on the left, the org name as h1 (font-display, 20px mobile / 28px sm+), a mono-dim city/state sub-line, the type badge below that, and an Edit button (h-9 px-2.5 sm:px-3, icon-only on mobile) anchored at the right. The Created timestamp moved from the old flowing header into Details (matches the Opportunity treatment — header chrome stays light; reference info lives in Details). The page now publishes `--ps-chrome-bottom` via useChromeBottom so dialogs anchor below the fixed chrome — third detail page to do so, meeting the rule-of-three trigger for a shared fixed-header shell extraction (queued). Body order is unchanged: Details (behind the shared DetailsCollapsibleHeader, default-collapsed), two hospital-only sections rendered only when org.type === 'hospital' and sitting between Details and Contacts (Privilege Roster and Opportunities list, both wrapped in bg-surface-well B containers per the two-level box convention with bg-surface record cards inside), an embedded contacts list (using the shared CardKebab for per-row Edit + Delete), Activity (toggle + 90-day + View All per the cluster-A universal), embedded Tasks section (shared TaskCard with hideParent + View All), and a destructive delete at the bottom that cascades to contacts and activities. Contacts.jsx is the cross-org contact list with Organization filter; Contact.jsx is the minimal detail page added in Slice 4 that mirrors Provider's shape — title, role, mailto email link, tel phone link, linked organization, notes, and a page-level destructive delete. Providers.jsx is the supply-pipeline list with Status, Specialty, and Show-archived filters. Funnel.jsx at `/funnel` is the read/navigate-only recruiting-funnel view over the same providers, grouped by pipeline stage so the supply pipeline reads at a glance — stacked collapsible stage sections at every width (no kanban; the demand-side kanban precedent is parked at this volume, and a single stacked layout keeps phone↔desktop consistent), with the seven active recruiting stages (Target → Active) shown first under an "Active pipeline" group header and the three off-pipeline stages (Inactive, Declined, Disqualified) below a muted "Off pipeline" header. Default-open is the live recruiting pipeline (Target through Onboarding); Active is collapsed because at 18 of 26 providers it would otherwise dominate the page, and the off-pipeline stages are collapsed for the same reason. Empty active stages still render their header and show a muted dash when expanded so the funnel shape is structurally visible; 0-count off-pipeline stages are hidden entirely. The page carries the suite's standard two-tier list-page chrome plus a third fixed row — a horizontally-scrollable chip strip of the seven active stages with their counts; tapping a chip jump-scrolls to that section and force-expands it if collapsed (chips never close; only the section headers toggle). The chip strip is included in the funnel's `--ps-chrome-bottom` calc via useChromeBottom so dialogs opened from this page anchor below all four bars (primary header + bar-2 + chip strip + conditional search). Cards reuse the shared ProviderCard with showStatus=false (the stage section is the status; the badge would be redundant) and no kebab handlers (status is set via the provider edit dialog on /providers or the detail page, not the funnel) but with the task pill kept verbatim, including the overdue-red treatment — the "stalled candidate with an overdue follow-up" signal is the point of carrying tasks here. Section-header counts render as plain text (e.g. "Contacted · 2 providers") so the pill/circle treatment stays reserved for tappable nav (the chip strip) and the overdue-task signal. Provider.jsx is the reference design for detail pages. A fixed, non-collapsing condensed header (photo, name on one line, contact row with city/ST + email/phone icons, badge row with position + specialty abbrev + provider pipeline status, Edit button icon-only on mobile / icon+text wide) sits below the primary PageHeader. Body section order is Details, then Onboarding, then Credentialing, then Hospital Standing, then Activity, then Tasks, then a footer Delete. The first three plus Hospital Standing form a "standing cluster" grouped above the Activity/Tasks workflow sections; all four use the two-level B box convention with bg-surface-well containers and bg-surface record cards inside. Details is the header-less primary-section collapsible (default collapsed) holding every editable field not in the header in a dense label-value grid, now driven by the shared DetailsCollapsibleHeader so the provider and organization detail pages match — distinct from CollapsibleSection, which is the labeled-sub-group pattern used inside sections. Onboarding sits above Credentialing to mirror the onboard-then-credential lifecycle. Both Onboarding and Credentialing follow ONE pattern, which is the new reference shape for detail-page sections: a diamond SectionHeader, then an always-visible centered status indicator, then a default-collapsed body behind the shared CollapsibleSection. Onboarding's status indicator is "N of 3 complete" plus a thin progress bar (denominator covers CV, references-satisfied-if-any-row-done, and background check; license/DEA are NOT counted here); its collapsed body is the catalog-driven checklist. Credentialing's status indicator is a centered three-line worst-status-per-group summary (Licenses / Credentials / Privileges) computed via deriveCredentialingStatus and expirationBucket — the count reflects rows in the worst bucket only (so "1 expired" means one row needs attention, not a total), empty groups read "none on file," and tone colors mirror the credentialing rows themselves; its collapsed body is the three subsection components (State licenses, Core credentials, Facility privileges) unchanged. Activity uses the universal toggle pattern described in the page-section opener above — "+ New activity" reveals LogActivityForm on demand, the feed defaults to the last 90 days, and a "View all" button routes to /activities for the unbounded archive; the same pattern now applies on Organization and Opportunity. The page publishes its total fixed chrome height (primary header + the measured condensed header) to a `--ps-chrome-bottom` CSS variable via the shared useChromeBottom hook, so dialogs opened from this page anchor below all fixed chrome rather than overlapping the provider header — the provider header stays visible during dialog open, per the suite convention that chrome sits above overlays for orientation. Opportunities.jsx is the demand-pipeline list with Stage, Specialty, State, and Source-partner filters; Opportunity.jsx is the detail page with hospital header, optional "via [partner name]" rendering, stage badge, and an Edit button. The body section order is Details → Provider Availability → Rate structure → GP modeler → Activity → Tasks → Delete. This order is INTERIM (completeness-driven) — Provider Availability is the finished section and leads; the unfinished Rate structure, GP modeler, and Activity sections were cascaded below to signal that, keeping their prior relative order. Final ordering is a polish-phase decision, not the current placement. The first section was renamed Overview → Details to align with every other detail page and now uses the shared DetailsCollapsibleHeader one-off (default-collapsed, header-less primary-section pattern, bare DetailGrid below when expanded, no card wrapper). The full rate-structure grid (shift defaults, bill rates, pay rates, on-call when enabled, travel costs), the live GP modeler, the activity feed and log-activity form, and the embedded Tasks section all sit below Provider Availability. Provider Availability itself sits in the B box convention — bg-surface-well container with full teal border + bottom gradient rule, carrying a compact Requirements readout ("Requires: State license · DEA · …" using REQUIREMENT_ITEMS labels, or muted "Requirements not defined" when required_items is null or empty) plus the tiered matching surface from the lifecycle-visibility slice — see the SuggestedProviders entry above for the full shape. Provider pool is filtered to non-archived only — every pipeline status, not just active, because onboarding and interviewing providers are real available capacity worth surfacing. Specialty is a constrained Select off the SPECIALTIES constant on both providers and opportunities so the comparison is safe strict-equality; it is a hard eligibility filter, not a verdict input, so credential gaps remain visually distinct from a wrong-person match. Honest indeterminate and blocked verdicts surface as-is — no fallback hides them or fakes readiness. A destructive delete sits at the bottom of the page. Tasks.jsx is the day-to-day follow-up surface — three tabs ("My open" filtered to the current user, "All open" across the team, "Completed (30d)") with a search-by-title input that filters within the active tab, a quick-complete checkbox on each open-task card, an overdue-styled due date when an open task is past due, and whole-card tap to the new Task detail page; Task.jsx is the minimal detail page added in Slice 4 mirroring Provider's shape — due date with overdue marker, assignee, parent link (with an "at {hospital}" sub-line for opportunity-parented tasks), notes, and a page-level destructive delete. The embedded TasksSection on each detail page (components/tasks/TasksSection.jsx) is the same render shape narrowed to a single parent: a "Show completed (30d)" toggle reveals the last-30-days completed tasks for that parent, the New Task button opens TaskFormDialog with the parent locked to the current resource, and editing an existing task still allows re-parenting freely. TaskFormDialog carries a hideDeleteAction prop that suppresses its in-dialog Delete button — the new Task detail page passes it (page-level Delete at the bottom is canonical there); list-context callers leave it unset and keep the in-dialog Delete visible so every entry point to a task has exactly one delete affordance. Activities.jsx at `/activities` is the new global activity archive shipped in the cluster-A polish-pass — surfaces every logged touch across the app. It uses the suite's two-tier list-page chrome (bar-2 title + description + Search and Filter icons; no Plus, because activities aren't created from a global page — they're logged against an entity). Below the chrome sits a coverage-framed two-card KPI strip — Quiet providers (active providers with no activity in 30+ days) and Quiet hospitals (org.type='hospital' with no activity in 30+ days) — chosen because the operationally-load-bearing question for an activity archive is "who am I neglecting" rather than "how busy was I." A third intended card (Quiet opportunities) is deferred until Opportunity active-stage definitions settle. Below the strip the feed groups activities into five time buckets (Today / This Week / Last Week / This Month / Older) with reverse chronological order within each, parent column always visible (unlike the section-on-detail-page ActivityFeed where the parent IS the page), and a slide-in filter sheet matching the list-page pattern (Parent type / Activity type / Date range / Sort). Empty state matches the suite-wide cluster-A shape (plain text, text-text-muted, font-mono text-xs uppercase, centered py-6). The page is a conceptual shell — designed for "looks right when sparse, scales when dense" and expected to be revisited and reworked once real activity history accumulates.

Utility files: lib/utils.js exports cn() for clsx + tailwind-merge. utils/constants.js holds ORGANIZATION_TYPES, CONTACT_ROLES, ACTIVITY_TYPES, US_STATES, PROVIDER_STATUSES (now a 10-stage list — target, lead, contacted, interested, interviewing, onboarding, active, inactive, declined, disqualified — each entry carries an optional description for the status picker's selection-helper hint; `credentialed` was removed because credentialing readiness is now computed at render time from the credentialing tables, not a manual status), POSITION_TYPES, SPECIALTIES with companion SPECIALTY_ABBR + specialtyAbbrFor helper (used in the provider detail header so Gastroenterology shows as "Gastro"), PROVIDER_SOURCES, OPPORTUNITY_STAGES with companion ACTIVE_OPPORTUNITY_STAGES (the four in-pipeline stages — lead, qualified, proposal, contracted — used as the suite-wide source of truth for "open" by both the Organizations card pill and the Home open-opportunities KPI), OPPORTUNITY_SETTINGS, TASK_PRIORITIES, TASK_STATUSES, the credentialing enums LICENSE_STATUSES, CREDENTIAL_TYPES, CREDENTIAL_STATUSES, PRIVILEGE_STATUSES, and REQUIREMENT_ITEMS (the seven keys deriveShiftReadiness gates on — `license`, the five real credential types sourced from CREDENTIAL_TYPES minus `other`, and `privilege`; their `value` strings must stay byte-identical to the keys the readiness helper checks, and the Requirements picker writes only these values into opportunities.required_items); labelFor (lookup by value) and descriptionFor (companion for the value+description shape on PROVIDER_STATUSES) are the helpers. utils/formatters.js holds Intl-based currency, date, and phone helpers plus fmtName. The phone helper was tightened to return a dash when the input has no digits, absorbing legacy AppSheet rows whose Phone Number column held the literal "() -" template. fmtName joins first + middle (rendered as-is, not forced to initial) + last + ", suffix" — so a provider stored with middle_name "A." renders "Diego A. Lim" and the same call against a contact record (which carries only first_name + last_name in the schema) renders "First Last" via the same graceful-degradation path. utils/storage.js (Phase 2) wraps supabase.storage.getPublicUrl so component code stays out of the supabase surface area, and exports an initialsFor helper for avatar fallbacks. utils/gp-modeler.js holds the pure compute() function and the setting-aware seedDefaults() — kept separate from the GPModeler component so the formula can be unit-tested later if Vitest gets added. utils/form.js holds the scrollToFirstError(formRef, errorFields) helper used by the long-form dialogs (ProviderFormDialog, OpportunityFormDialog) to scroll the first invalid named field into view and focus it on submit-time validation failure.

A scripts/ folder (Phase 2) holds the one-time import-from-appsheet.js — a local-only Node script that reads the AppSheet snapshot workbook from the suite-level _reference/ folder, normalizes specialty and position type, parses City/ST, and upserts providers, organizations, and opportunities by appsheet_id. Idempotent, dry-run required, runs locally with the service role key in env. Image binaries from _reference/appsheet-images/ upload to the public-read storage buckets when present. The script has been run live once: 33 rows in (3 organizations, 26 providers, 4 opportunities), all 6 organization images and 26 provider photos uploaded, both Billings opportunities tagged with Medicus as source partner via the SOURCE_PARTNER_OVERRIDES map. A sibling upload-onboarding-templates.js was added alongside 0010 — local-only, dry-run-aware, service-role-required, idempotent via upsert: true on the storage upload. It reads the four blank onboarding-template PDFs from _reference/onboarding-templates/ at the suite root and uploads them into the credentials bucket at the same paths the 0010 catalog rows seed. Run once after 0010 applies and after the PDFs are placed in the suite _reference/ folder.

Database: eleven migrations. supabase/migrations/0001_initial.sql created organizations, contacts, activities, the shared updated_at trigger, and Phase 1 RLS policies. supabase/migrations/0002_pipelines.sql (Phase 2) adds providers, opportunities (with the full six-bill-side / five-pay-side rate structure plus shift defaults, on-call window, and a modeling_assumptions jsonb column), tasks, and placements; adds appsheet_id, logo_path, image_path, tourist_site_url, and long_description to organizations; wires the Phase 1 placeholder activities.opportunity_id and activities.provider_id columns to real foreign keys; turns on permissive Phase 1-style RLS on the four new tables; creates the organization-logos and provider-photos public-read storage buckets with authenticated-write policies; and seeds one organizations row for Medicus Healthcare Solutions. supabase/migrations/0003_travel_costs.sql adds four columns to opportunities (ps_covers_travel boolean default false, plus airfare / hotel / rental rate estimates) with an asymmetric CHECK constraint that requires the rate columns to be null when the flag is false but does not require them to be populated when the flag is true — matching the daily-use case where the flag flips before specific rates are priced out. Credentialing was originally penciled as 0003 but slipped to 0004 as a result. supabase/migrations/0004_credentialing.sql (Phase 3, slice 3a) adds three layered credentialing tables — provider_licenses (state medical licenses, provider-level, state CHECK-constrained to the 50 US states + DC), credentials (provider-level core credentials — board cert, DEA, BLS, ACLS, malpractice, other — with a free-text `label` required when type='other' so multiple "other" rows are distinguishable), and facility_privileges (per-hospital bridge with the full lifecycle status set: pending, active, expired, denied, withdrawn) — plus the private `credentials` storage bucket with authenticated-only SELECT/INSERT/UPDATE/DELETE policies (no anon read) so documents are served via createSignedUrl. supabase/migrations/0005_provider_status_values.sql swaps the providers.status CHECK constraint to the new 10-value list (target, lead, contacted, interested, interviewing, onboarding, active, inactive, declined, disqualified) with NOT VALID so existing 'credentialed' rows are grandfathered — those rows must be reassigned manually on next edit; a future VALIDATE CONSTRAINT pass can formally validate the historical data once all stale rows are gone. supabase/migrations/0006_credentialing_application_dates.sql adds a nullable application_date date column to provider_licenses and credentials so all three credentialing tables share the same four-state computed status model (applied → active → expiring → expired). supabase/migrations/0007_opportunity_requirements.sql (Phase 3, slice 3b) adds a nullable required_items text[] column to opportunities — no default, no CHECK; the column drives the readiness helper, which treats null and empty array identically as "not assessable yet" (REQUIREMENTS_UNDEFINED). The trust model matches modeling_assumptions — the Requirements picker is the only writer and only writes valid keys drawn from REQUIREMENT_ITEMS in constants.js, so a CHECK was deliberately omitted. supabase/migrations/0008_onboarding.sql (Phase 3, slice 3b) adds the two onboarding tables — onboarding_item_types (the catalog, seeded with `cv`, `references` repeatable, and `background_check`) and onboarding_items (per-provider persisted rows with done, completed_date, and an optional document_path into the existing private credentials bucket from 0004 — no new bucket). onboarding_items.item_key is a real FK into onboarding_item_types(key) with `on delete restrict`; this is the first editable-catalog FK in the app and is the template for future admin-managed list tables. supabase/migrations/0009_placements_selected_status.sql (Phase 4, slice 4b) relaxes the placements.status CHECK to add 'selected' alongside the original five values (proposed, accepted, active, completed, cancelled) — additive only so the future Scheduler's lifecycle values are preserved. The default stays 'proposed' at the DB level; the CRM's Select action writes 'selected' explicitly. supabase/migrations/0010_onboarding_catalog_expansion.sql (Phase 3, on top of slice 3b) adds two nullable columns to onboarding_item_types — template_path (a path within the credentials bucket where the blank fillable template PDF lives) and version (the template's version identifier, e.g. '1.0' for in-house forms or '2024-03' for an IRS-revisioned W-9) — and seeds six new catalog rows: four document-shaped items that carry templates (Physician Services Agreement, Physician Attestation, IRS Form W-9, Direct Deposit Authorization) and two information-shaped items with no template (Emergency Contact Information, Malpractice Insurance Certificate). HIPAA Acknowledgment is intentionally deferred to a future migration — the template language has not been drafted. supabase/migrations/0011_role_based_rls.sql (suite identity layer) creates public.profiles (one row per auth.users id, role admin/recruiter/provider, provider_id FK gated by a role-link CHECK and a one-login-per-provider unique partial index), three SECURITY DEFINER helpers (is_staff(), current_user_role(), current_provider_id()) reusable verbatim by future apps, and replaces every permissive policy with role-aware policies per the table classes documented in the Auth section below. Both staff seats seeded as admin via a SELECT against auth.users by email so any account that hasn't logged in yet is silently skipped; the migration's verification step reports back which seats were actually seeded so a gap is visible. The plan-of-record lives at docs/RLS-PLAN.md alongside the migration. Migrations are immutable once applied — never edit any of them again, ever; new changes require new numbered files.

## Data model and integrations

Twelve tables once 0008 is applied. organizations carries name, type (hospital, locums_partner, or other), website, address fields, notes, and now image / recruiting columns (logo_path, image_path, tourist_site_url, long_description) plus appsheet_id. The hospital-flavored image and recruiting columns remain on the table for every row but are only surfaced in the create/edit form when type is hospital or other; partner orgs only need name, website, address, contacts, and notes. contacts hangs off organization_id with name, title, role, email, phone, notes. providers carries name parts, contact info, NPI, specialty, position_type, home city/state, photo_path, travel-booking conveniences, status (the 10-stage recruiting pipeline updated by 0005), source, an archived flag, and appsheet_id. opportunities hangs off organization_id (the hospital) with an optional source_partner_id (the LOCUMs partner organization, e.g., Medicus — a relationship AppSheet never modeled, so legacy data is seeded by hand during import), title, name, position_type, specialty, setting, location, dates, the full bill-side and pay-side rate structure, an on-call window, a ps_covers_travel flag plus three travel-rate columns added in 0003, a jsonb modeling_assumptions column for the GP modeler, a required_items text[] added in 0007 (the flat list of credentialing items deriveShiftReadiness gates on — null/empty reads as "not assessable yet," not "ready by default"), pipeline stage and probability, next_action_date, notes, and appsheet_id. tasks carries title, due_date, status, priority, an assignee, and optional links to organization, opportunity, or provider. placements bridges provider_id and opportunity_id with start/end dates, status, and per-placement pay_rate / bill_rate. Migration 0009 (Phase 4b) relaxed the status CHECK to add 'selected' alongside the original five values; 'selected' is the CRM-authored value and is written by usePlacements when the recruiter commits a provider to an opportunity. Under the placement-boundary decision made during Phase 4a planning and made concrete by 4b, the CRM's responsibility ends at "this provider is eligible and committed to this opportunity"; the future Scheduler app owns shifts, dates, and daily pay/bill rates. The placements.start_date, end_date, pay_rate, and bill_rate columns stay null on CRM-created rows and are retained as a possible place for the Scheduler to pass summary info back to the CRM for display, possibly removable later if that passback never materializes. activities is the polymorphic touch log; with 0002 applied, all four parent FK columns now carry REFERENCES, and the Phase 1 CHECK enforcing exactly one parent is unchanged. The slice-3a credentialing tables are layered: provider_licenses (state medical licenses, the matching read path), credentials (provider-level core credentials with a free-text label required when type='other'), and facility_privileges (per-hospital bridge, the per-placement gate). All three share the same four-state computed status model derived from dates at render time (expired → active → applied → pending); the DB status column is kept in sync on writes but display never reads it for the lifecycle states. Facility privileges also carry two terminal outcomes (denied, withdrawn) that the kebab can set explicitly — terminal stored statuses override the date-driven display so a denied privilege never renders as Pending or Active. The slice-3b onboarding tables are layered too: onboarding_item_types is the catalog (key, label, repeatable boolean, sort_order, plus the 0010 additions template_path and version — the latter pair carry path and version identifier for catalog items that ship with a blank fillable template, null for everything else) and onboarding_items is the per-provider persistence layer (provider_id cascade, item_key FK into the catalog with on delete restrict, done boolean with completed_date auto-stamped on toggle-on and cleared on toggle-off, optional document_path into the existing credentials bucket). The catalog now seeds nine rows — three original intake items (cv, references repeatable, background_check) plus six 0010 additions (four document-template items: physician_services_agreement, attestation, w9, direct_deposit; two information-shaped items with no template: emergency_contact, malpractice_coi). HIPAA Acknowledgment is queued for a future slice, parked until the language is drafted. There is still no admin UI; new types go in by manual insert.

Audit columns (created_at, updated_at, created_by referencing auth.users) appear on every user-editable table. UUIDs everywhere. No soft-delete — hard delete with cascade. CHECK constraints validate enum-like text columns and rate non-negativity; an opportunities.on_call_enabled flag gates the on-call rate columns through a CHECK requiring rates to be NOT NULL when enabled, while the 0003 ps_covers_travel flag gates the three travel rate columns through the inverse asymmetric CHECK requiring rates to be NULL when disabled.

External integrations today: Supabase auth and Postgres, plus three storage buckets — organization-logos and provider-photos (public-read, created in 0002, image rendering uses supabase.storage.getPublicUrl so URLs need no signing) and credentials (private, created in 0004, documents served via supabase.storage.createSignedUrl with a 5-minute expiry, authenticated SELECT/INSERT/UPDATE/DELETE only — now also hosts blank fillable onboarding templates under templates/{item_key}/v{version}.pdf alongside the per-provider per-row {row_id}/{uuid}.{ext} document paths, with the catalog's template_path column pointing at the blank for each document-shaped row). The QBO proxy at qbo-proxy.finance-providersolutions.workers.dev is still the planned cross-app data source for opportunity actuals but is not wired up yet — that integration is optional and deferred to the later Home-enrichment pass, no longer bundled into Phase 2. AppSheet remains the legacy system providers use for shift logs; the appsheet_id columns now exist on providers, organizations, and opportunities for stable matching when the import script runs. There is no two-way sync, will never be one — AppSheet is being retired, not synchronized.

The Phase 3 credential-alerts Edge Function that emails a daily expiration digest to all.provider.solutions@gmail.com via Resend is still pending — that ships in slice 3c. Slice 3a manual entry plus the cross-provider expiration roll-up at /expirations have shipped.

## Auth and core domain rules

Auth is email OTP — Supabase emails a 6-to-10 digit code (length is configurable in the Supabase dashboard; the input accepts the full range so a project-side change does not lock anyone out). The login flow used to be magic link; recent commits switched it to OTP because the code flow stays inside the installed PWA without bouncing through Mail.

RLS is role-based as of migration 0011. The permissive Phase-1 posture (any authenticated user can do anything) is retired. The new posture is built around a single suite-wide identity table — public.profiles — with one row per auth.users id carrying role (admin, recruiter, or provider) and, for providers, a provider_id foreign key into providers. The provider role is gated by a CHECK constraint requiring provider_id to be populated; non-provider roles must leave it null; a unique partial index on provider_id enforces one-login-per-provider. Three SECURITY DEFINER helpers — public.is_staff(), public.current_user_role(), public.current_provider_id() — read the calling user's profile without engaging RLS (so a profiles policy can call is_staff() without recursion). These helpers are the reusable scoping primitives the future provider portal and Scheduler will consume verbatim; the universal rule for any provider-scoped table in any backend app is two policies — staff full access via is_staff() plus provider SELECT scoped on provider_id = current_provider_id(). Per-table policy classes today: organizations and onboarding_item_types are reference-readable (any authenticated user can SELECT; only staff write); contacts, activities, opportunities, and tasks are staff-only (the portal does not surface any of them, including provider-id-pointing rows which are recruiter notes about the provider, not for them); providers is staff-full plus a provider own-row SELECT; provider_licenses, credentials, facility_privileges, and placements are staff-full plus provider own-records SELECT keyed on provider_id; onboarding_items is staff-full plus provider SELECT/INSERT/UPDATE on own rows (DELETE intentionally withheld — the portal "uncomplete" flow flips done to false). Both staff seats are seeded as admin (finance.providersolutions@gmail.com, exec.providersolutions@gmail.com); the recruiter role is reserved in the enum for future team members who should not also manage other accounts; viewer is intentionally not yet defined. Provider-scoping is in place at the schema level but has NOT been verified end-to-end against a real provider account — Reed-as-provider (separate profile under his personal email reedhogan@mac.com, scoped to his providers row) is deferred to the portal slice and will be the first real provider-login test case. The 'credentials' storage bucket is still permissively readable by any authenticated user and is intentionally left as-is for this slice — path-scoped storage policies depend on the portal's chosen path convention and land with the portal slice. Anonymous access remains blocked everywhere.

Once Phase 2 ships, every new provider, organization, and opportunity goes into the CRM exclusively — AppSheet is frozen for those record types. AppSheet still receives shift logs from providers until the future provider portal replaces that workflow.

Migrations are immutable once shipped. Errors must surface via sonner toast with the full error logged to console — never silently caught. Money is rendered with Intl currency formatting; estimates are italic with a leading tilde and the warning color. localStorage keys are prefixed ps-crm-. Routes are one per page; detail pages take their parent id as a URL param. Plain JSX, no TypeScript.

Destructive actions go through the shared ConfirmDeleteDialog wrapper, which restores focus to the trigger element on cancel rather than dropping it to body. Long-form dialogs (ProviderFormDialog, OpportunityFormDialog) use a sticky save bar on phone and pair toast.error with scrollToFirstError to surface validation failures. Required-field labels carry an inline red asterisk via the Field component's required prop whenever a field triggers a validation error in the submit handler.

## Known issues and latent risks

opportunities.next_action_date is empty across ALL opportunities and is not expected to be populated (informal two-user workflow; Jason does not anticipate using it). Three surfaces depend on it and are therefore inert or degraded: the dropped V6A "stale pipeline" home concept (its neglect signal had no data — see DESIGN-NOTES), the /opportunities list "Next action (soonest)" sort (operating on empty data, so effectively a no-op order), and the next_action_date field still on the create form. Open question for when real usage settles: whether to remove the field and its dependent surfaces entirely. Flagged so a future "why is this sort doing nothing" investigation finds the cause in one place.

Role-based RLS is in place at the schema level but provider-scoping has not been verified end-to-end against a real provider account yet — Reed-as-provider in the portal slice will be the first real test case. The 'credentials' storage bucket also remains permissively readable by any authenticated user, deferred to the portal slice where the path convention can be fixed and policies path-scoped in one pass. Both gaps are deliberate scope boundaries, not known bugs, but they mean the suite is NOT yet safe to grant a provider portal account — that gate moves with the portal slice, not this one.

Phase 4b has no selection history or audit trail. Un-selecting a provider hard-deletes the placements row behind a deliberate confirm dialog; there is no record of the prior selection afterward. Acceptable for the current two-user workflow; revisit if recruiting later wants a trail of "who was once committed and then released." The 'cancelled' placement status is intentionally excluded from what counts as Selected so the future Scheduler can wind a row down without it lingering as Selected. The Apply action (Selected → Applied write, creating a facility-privilege application from the matching UI) was deliberately not built in 4b — facility-privilege rows are still created only through the provider detail page's credentialing section.

Provider rows with the legacy 'credentialed' status value (from before 0005 applied) are grandfathered by 0005's NOT VALID constraint. They render with the fallback badge style via STATUS_BADGE_FALLBACK in src/pages/Providers.jsx. Editing such a row will fail the new CHECK unless the same edit also moves status to a value in the new 10-stage list — that failure surfaces via the existing toast.error path and is the prompt to reassign the status. Once all stale rows are gone, ALTER TABLE public.providers VALIDATE CONSTRAINT providers_status_check can be run manually as a someday cleanup.

Organization deletion cascades to contacts and activities through the schema, gated by a styled AlertDialog confirmation in the UI. There is no soft-delete and no undo. A misclick on a populated organization is destructive.

Activity type sms is in the schema and ACTIVITY_TYPES list but has no UI optimizations beyond the icon. LogActivityForm is now polymorphic and attaches cleanly to organizations, providers, and opportunities; the remaining gap is contacts — there is still no way to log an activity against a contact directly. Slice 4 added a Contact detail page, but the activity-against-contact link requires a schema change (activities currently has no contact_id link to a real contact record beyond the Phase 1 placeholder), so the gap persists. The minimal detail page does not try to fix it.

The Login.jsx OTP code step has a length minimum of 6 digits; if Supabase project settings ever drop below 6 the form will reject all valid codes. The pattern is set wide (6 to 10 digits) to absorb the documented configurable range, but a 4-digit setting would silently break login.

The Tasks page assignee picker is a simple "Assign to me / Unassigned" toggle rather than a real team-member picker. The schema's tasks.assignee_id references auth.users, which the Supabase JS client cannot enumerate without the service role key. The proper picker waits for the Phase 2-plus profiles table; until then, all tasks created in the UI are either assigned to the current user or left unassigned. Tasks created with another user's UUID via the API or migrated from elsewhere will still surface correctly in the "All open" tab, just without a name attached.

The Provider create/edit submit handler enforces "at least one of first or last name is filled" but falls back to the literal string "Unknown" for whichever field is left blank when building the database payload. A row can therefore land with first_name "Bob" and last_name "Unknown" (or vice versa). Three viable fixes — allow null in the DB schema, require both fields filled in the UI validation, or build the payload with null instead of "Unknown" — and the right choice depends on how Phase 3 credentialing and Phase 4 matching will use these fields. Parked product decision.

Token drift between this CRM and ps-app-dashboard is a manual sync today. tokens.css here mirrors the dashboard's index.css by hand. When a third app joins the suite, tokens extract to a shared package; until then, dashboard token edits must be ported to CRM by hand or visual divergence creeps in.

The light-mode --surface-well value (#cfdeec) is provisional. In dark mode the well + record-card elevation reads cleanly with strong contrast across every text role; in light mode the accent-label-on-container contrast computes at ~3.47, below WCAG AA's 4.5 for normal text. The other text roles (text-dim, accent on the white record cards) stay safely above AA. Records-vs-container separation reads strongly in light mode. The fix belongs in the suite-level light-mode tuning pass (already tracked at the suite level for similar status-color contrast issues) rather than as a CRM-only hotfix, since the tokens are mirrored across both apps.

The detail-page top padding hard-codes the header height as 58px plus safe-area-inset-top in inline styles. If the header height changes, every page needs an edit. A CSS variable would centralize this.

The new Onboarding and Credentialing sections on the provider detail page default to collapsed. That trades upfront density for a calmer scan, but it adds one tap to the workflow of arriving from the /expirations roll-up to act on a specific credential — the link lands on the provider page with the Credentialing body still closed, so the user has to expand "Provider Credentials" before the row they came for is visible. A deep-link mechanism that opens the right collapsible (and ideally scrolls to the specific row) is queued for the polish pass, not fixed now.

Hospital Privilege Roster rows fit cleanly at 380px on current seed data because names are short and the roster has no per-row action button (so the row has more horizontal slack than the opportunity-page rows). If real provider names grow long enough to crowd the right-aligned status indicator, the planned treatment is recorded in docs/DESIGN-NOTES.md (a polish-pass design-decisions ledger that owns this kind of presentation reconciliation across the app).

## Design system at a glance

Dark default. Background #0b1c2e (deep navy), surface #122540 raised one level, surface2 #1a3050 raised two levels. Accent is teal #7ee8e8 with a brighter hover variant #00e0ff and a near-transparent fill accent-dim. Income green #3ecf8e for active, current, won states. Danger red #ff8080 for lost, expired, COGS. Warning amber #c8a840 is CRM-only — used for caveats (on-call cap notes, unsaved-changes pills) and soon-expiring credentials in Phase 3. A new --profit token #2dd4bf (dark) / #0e8c80 (light) registered in piece 2 is the headline color for positive gross-profit values on the opportunity detail page — a deeper, more saturated teal than the #7ee8e8 accent (which reads as a label tone) and distinct from #3ecf8e income green; it's the bottom-line/outcome color, sign-gated (profit flips to danger red when negative). Exposed as a `profit` color in tailwind.config (consumers currently route through text-[var(--profit)] until a clean build picks up the named utility — see DESIGN-NOTES on the Tailwind-v3 opacity-modifier seam). Light theme is a separate :root block under data-theme="light"; ThemeToggle persists the choice to localStorage.

Three fonts. DM Serif Display for h1 and h2 only (font-display utility). DM Sans for body (font-sans, default). DM Mono for numbers, IDs, and small-caps labels (font-mono). Mono uppercase with wide letter-spacing is the recurring label and metadata treatment throughout.

Radius is 5px globally via the --radius variable.

Header icon buttons are 36×36, slightly smaller than the dialog close X at 40×40 — a context-specific choice justified by the dedicated chrome strip where no adjacent interactive elements compete for thumb taps.

List pages render a two-tier header. Below the primary header sits a second fixed bar carrying the page title and a sm+ description on the left and a three-icon action group on the right — Search, Filter, Plus — at the same 36×36 chrome-strip sizing. A "Clear" mono link appears in the second tier when any filter or search is active. A conditional third bar slides down below the second tier when search is toggled, hosting the search input. The filter sheet slides in from the right via the same Radix-style portal pattern as the Navigation drawer and contains per-page filters plus a sort row. List pages compute body padding dynamically — 58 + 56 + (52 if search open) + safe-area-inset-top — distinct from the 58px-only hardcode that non-list pages still use; that hardcode is unchanged and remains a known-issue item to centralize someday.

Shadcn-style overlays (Dialog, AlertDialog, the list-page filter sheet) all use z-50 and rely on DOM-order tiebreak for stacking among themselves so that Radix-portaled popovers like Select dropdowns render above the overlay that triggered them; chrome bars at z-150 (Bar 2 / Bar 3) and z-200 (the primary header) sit above overlays by design so the user can still orient on what page they are on while an overlay is open. The shared Dialog primitive anchors its top below all fixed chrome by reading a `--ps-chrome-bottom` CSS variable that pages publish via the useChromeBottom hook in src/hooks/useChromeBottom.js — list pages and the /expirations page set it to primary header + bar-2 + optional bar-3 (tracking searchOpen live so the variable updates when bar-3 slides in), the Provider, Opportunity, and Organization detail pages each set it to primary header + their measured condensed header (Organization adopted the pattern in the cluster-A polish-pass, replicating the same ref + ResizeObserver + useChromeBottom mechanism inline — three inline replications now meet the rule-of-three trigger for extracting a shared fixed-header shell, queued as its own follow-up), and pages without page-owned chrome leave the variable unset so the dialog falls back to anchoring below the 58px primary header alone. Dialogs open below chrome, not under it; chrome stays visible during open. The shared Dialog primitive now also owns dialog HEIGHT, not just top anchoring: it sets maxHeight to calc(100dvh - var(--ps-chrome-bottom) - 2rem) and lays out as flex flex-col, so every form dialog's body scrolls internally while its footer (Cancel/Save) is pinned outside the scroll region by layout. This replaced the prior per-consumer max-h-[90vh] pattern, which did not subtract the chrome offset and let tall dialogs run their save bar off the bottom of the viewport. All nine form-dialog consumers were restructured to the shared header / scrollable-body / pinned-footer shape; ConfirmDeleteDialog (AlertDialog, a separate primitive) is unaffected.

All five list pages render rows as cards rather than as shadcn Tables. The card pattern is one card per row at every width with no two-up grid, but the card itself has two distinct layouts joined at the md breakpoint (768px). Below md, the card is a stacked block: a logo-slot thumbnail on the left and a meta block vertically centered to its right. At md and above, the card switches to a single horizontal row: a smaller thumbnail on the far left, content cluster (or clusters) in the middle, and a right-anchored indicator cluster. The breakpoint switch is clean — no intermediate transitional state, fully one layout below md and fully the other above. Whole-card tap navigates to the detail page; Enter and Space on the focused card do the same. The kebab opens a dropdown with Edit (opens the form dialog in edit mode), an optional middle item for lifecycle actions like Archive, and Delete (routes through the shared ConfirmDeleteDialog with cascading-delete warning when applicable); the menu auto-closes on selection. The kebab itself lives in the shared CardKebab component so all five pages stay in sync.

The card grammar is consistent across pages: the logo slot is the resource's strongest available image; the primary identifier renders in accent teal font-display; secondary descriptors render in white sans or mono dim; the badge slot on the right is reserved for lifecycle state (stage, status) and stays empty for resources without one. Per-page variations exist where the data warrants. Opportunities cards have a 4-row mobile pattern (position·spec·setting / title / hospital / city·ST + stage badge) with the org logo as the thumbnail. Tasks cards omit the logo slot entirely because tasks carry no identity image; the meta block claims full card width, the row 1 mono line carries priority as a cap label (with high priority in text-warning) alongside the quick-complete checkbox and kebab, the task title is the accent-teal primary on row 2, the parent identifier with its mono cap type prefix sits on row 3, and opportunity-parented tasks add an "at {hospital}" sub-line beneath the opportunity title because the title alone rarely identifies which hospital a task belongs to. Contacts cards use the parent org's logo (since every contact has a required parent org) and a 4-row mobile pattern with the contact name as the primary, a "ROLE at" lead-in below the name that reads as a preposition flowing into row 3's organization name, and a title row at the bottom — rows 2 and 4 hide gracefully when role or title is absent; email and phone live on the detail page as mailto/tel links, not on the card. Providers cards use the provider's own photo (the strongest logo-slot case in the suite) and a 3-row mobile pattern (position·spec / name / city·ST + status badge); the kebab carries Archive/Unarchive as a middle item between Edit and Delete, and an archived provider renders at opacity-50 on the whole card. Organizations cards use the org's own logo, a 3-row mobile pattern (type mono cap / name / city·ST), and a parent-scoped pill counting active opportunities only — lead, qualified, proposal, and contracted; filled and lost are terminal and excluded. Org names systematically need to wrap to two lines because the disambiguating "Medical Center" or "Health System" suffix is exactly what single-line truncate cuts; card height varies within the Organizations page as a consequence, which the design accepts.

Type, role, and similar categorization signals render as mono cap labels rather than as colored chips in the badge slot, even when a colored palette already exists for them — the badge slot stays semantically pure as the lifecycle-state slot across the suite. The colored TYPE_BADGE palette stays alive on the Organization detail page header where chip treatment is appropriate for a structured detail surface.

The parent-scoped pill on Opportunities, Providers, and Organizations cards follows one shared shape: a single page-level fetch of the relevant resource (open tasks for Opportunities and Providers, opportunities for Organizations) gets bucketed into a per-parent map by useMemo so each card reads its count from local state — no per-card query and no N plus one. The pill is a small outlined chip. The numeral renders in accent teal normally; on the task pills it switches to danger red with medium weight when at least one linked open task is overdue. Zero counts hide the pill entirely. The Organizations opportunity pill spells the unit word ("opportunity" / "opportunities") in full at md+ rather than abbreviating. The Provider-card task pill carries the unit word ("task" / "tasks") at every width now, including mobile — the overlap fix below and the status-badge suppression on the funnel freed enough row-1 space that the digit alone was unnecessarily terse, and the wider pill reads better in both contexts.

ProviderCard is a single shared component used by both the Providers list and the Funnel, parameterized by a showStatus prop and by the presence or absence of edit/archive/delete handlers — the kebab disappears entirely in read/navigate-only contexts (the funnel). A latent vertical-rhythm bug was found and fixed during the funnel build: a `-mt-1` on the H3 name row was pulling the line-box into the position·spec row above, overlapping by 4px on every card that lacked a tall row-1 right-side element. Removed; the card now uses a small positive gap between rows instead. The bug had been present on the Providers list too at lower visibility, and is now resolved on both pages.

Brand components are hand-mirrored from the financial dashboard for visual recognizability. Non-brand UI (tables, forms, dialogs, lists, detail panes) uses Tailwind utilities and shadcn/ui primitives freely with no dashboard-class-pattern constraint.

Standing-cluster detail-page sections use the two-level box convention. Level 1 is the section container — bg-surface-well (#061626 in dark mode, a darker "well" shade than the page bg) with a full teal border (border-accent) and the bottom accent-to-transparent gradient rule; the section header sits above the container as a diamond ◆ SectionHeader (or, for the Details section, as the shared DetailsCollapsibleHeader one-off), and the section's always-visible summary line sits INSIDE the container at the top, above the collapsible body. The teal border is load-bearing because the well's fill-vs-page luminance contrast is intentionally low — without the teal edge the container would blend into the page. Level 2 is the record cards inside — bg-surface (#122540, raised above the well) with the standard border-border outline. Records-are-cards is conditional, not unconditional: Level-2 carding applies when records sit DIRECTLY in a section container (Org Privilege Roster rows, Org Opportunities rows, the three credentialing record groups, the per-hospital cards on Hospital Standing), but when records are already nested inside an intermediate grouping (the Provider Availability lifecycle tiers each wrap their provider rows in their own CollapsibleSection), the rows stay plain to avoid box-in-box-in-box clutter. The convention applies today to: Provider Onboarding, Credentialing, and Hospital Standing; Organization Privilege Roster and Opportunities; Opportunity Provider Availability.
